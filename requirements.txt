APScheduler==3.10.4
Babel==2.17.0
beautifulsoup4==4.13.4
dateparser==1.2.2
httpx==0.28.1
python-dateutil==2.9.0.post0
python-dotenv==1.1.1
python-telegram-bot==21.11.1
rapidfuzz==3.13.0
recurrent==0.4.1
SQLAlchemy==2.0.43
pytest==8.4.1
pytest-asyncio==1.1.0
google-api-python-client==2.184.0
google-auth==2.50.0
google-auth-oauthlib==1.2.2
# H2-047 Wave 2: google-genai + mem0ai + qdrant-client + the BM25/spaCy
# extras they pulled in are gone. LocalMemoryService (Wave 1) replaced
# mem0; brain_router replaced AIService. Only the Google OAuth/API client
# libs stay (used by nexus-email / calendar / contacts MCP servers).
openai-whisper

# Phase 2.5a MCP servers (2026-05-12)
mcp==1.27.1
qrcode==8.2
pdfplumber==0.11.9
python-docx==1.2.0
openpyxl==3.1.5
reportlab==4.4.10
pypdf==6.9.2
pypdfium2==5.6.0

# Phase 5 browser agent (2026-05-12)
playwright==1.59.0
playwright-stealth==2.0.3

# Phase 7 dashboard (2026-05-12)
fastapi==0.115.6
# starlette pinned to the version FastAPI 0.115.6 was tested against; the
# environment otherwise resolves starlette 1.0.0 (incompatible — drops on_startup kwarg).
starlette==0.41.3
uvicorn==0.32.1
Jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.12
